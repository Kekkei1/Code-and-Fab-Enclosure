#include <Servo.h>

Servo servo1;
const int buttonPin = 13;

void setup() {

  servo1.attach(3);
}

void loop() {
  if (digitalRead(buttonPin) == HIGH){
    servo1.write(100);
  } else {
    servo1.write(90);
  }
 
}
